import { createContext } from 'react';

/**
 * Auth Context Definition
 * Separated to comply with Fast Refresh requirements
 */
export const AuthContext = createContext(undefined);

// Made with ❤️ for Interview Preparation
